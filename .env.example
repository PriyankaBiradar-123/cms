# Database configuration
DB_SERVER=localhost
DB_USERNAME=root
DB_PASSWORD=root
DB_NAME=cms_db

# App base URL must end with a slash
BASE_URL=http://localhost/cms/

# Optional for Docker/Railway
PORT=8080
